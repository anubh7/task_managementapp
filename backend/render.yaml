services:
  - type: web
    name: task-management-backend
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        from: env
    rootPath: .
