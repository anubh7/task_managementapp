services:
  - type: web
    name: task-management-backend
    env: node
    plan: starter
    rootPath: .
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        from: env
